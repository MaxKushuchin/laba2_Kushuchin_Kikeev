#include <iostream>
#include <set>

int main()
{
    int n, num;
    std::cin >> n;

    std::set<int> coll;

    for(int i = 0; i < n; ++i){
        std::cin >> num;
        coll.insert(num);
    }

    std::cout << coll.size();
    return 0;
}
